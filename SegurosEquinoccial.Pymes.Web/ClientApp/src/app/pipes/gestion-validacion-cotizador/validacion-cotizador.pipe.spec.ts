import { ValidacionCotizadorPipe } from './validacion-cotizador.pipe';

describe('ValidacionCotizadorPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidacionCotizadorPipe();
    expect(pipe).toBeTruthy();
  });
});
