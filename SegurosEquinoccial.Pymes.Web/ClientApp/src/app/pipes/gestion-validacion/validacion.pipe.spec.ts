import { ValidacionPipe } from './validacion.pipe';

describe('ValidacionPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidacionPipe();
    expect(pipe).toBeTruthy();
  });
});
